
#热部署
spring.devtools.restart.enabled=true


server.port=8080

#  默认是"/"
#  默认访问方式: http://localhost:8080/hello
#  访问方式:  http://localhost:8080/st/hello
server.servlet.context-path=/st

# 应用名字
# 作用: springcloud中, 用于服务的注册与发现
spring.application.name="stApps"

# path  vs. name?


############################################################
##                  myssql configure                      ##
############################################################
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=songtaoxy
spring.datasource.url=jdbc:mysql://localhost:3306/springboot?useUnicode=ture&characterEncoding=UTF-8&serverTimeZone=UTC
# 数据库, 默认采用数据源HikariDataSource

# sql脚本初始化 2.5.0
# 源码中可知,不能写username和password, 但好多文档说要写
# DataSourceInitializationConfiguration
#spring.sql.init.username=root
#spring.sql.init.password=songtaoxy
spring.sql.init.schema-locations=classpath*:sql/*.sql
spring.sql.init.data-locations=classpath*:sql/data/*.sql